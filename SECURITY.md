# 🛡️ Security Implementation (มาตรการความปลอดภัย)

เอกสารฉบับนี้อธิบายภาพรวมของมาตรการรักษาความปลอดภัยที่นำมาใช้ในระบบ เพื่อให้มั่นใจในความปลอดภัยของข้อมูลและการเข้าถึงระบบ

## 🔑 Authentication & Authorization (การยืนยันตัวตนและการอนุญาต)

### 1. Google OAuh 2.0 (Google Sign-In)
ระบบรองรับการเข้าสู่ระบบผ่านบัญชี Google เพื่อความสะดวกและปลอดภัย
*   **Mechanism**: ใช้ `id_token` ที่ได้รับจาก Google ในการแลกเปลี่ยน Session Token ของระบบ
*   **Integrity**: ตรวจสอบความถูกต้องของ Token signature และ Audience (Client ID) กับ Google Server ฝั่ง Backend เสมอ

### 2. Token Management
ระบบใช้มาตรฐาน **JWT (JSON Web Token)** ในการจัดการ Session
*   **Access Token**: อายุสั้น (Short-lived) ใช้สำหรับเข้าถึง API Resource
*   **Refresh Token**: อายุยาว (Long-lived) ใช้สำหรับขอ Access Token ใหม่แบบอัตโนมัติ (Token Rotation)
*   **Session Binding**: เชื่อมโยง Token กับข้อมูลผู้ใช้และ Device ID เพื่อป้องกันการถูกขโมยใช้ (Session Hijacking)

### 3. Secure Cookies
การจัดเก็บ Token และข้อมูลสำคัญจะทำผ่าน **HTTPOnly Cookies** เท่านั้น
*   **HTTPOnly**: ป้องกัน JavaScript ฝั่ง Client อ่านค่า Cookie (ป้องกัน XSS)
*   **Secure**: บังคับใช้ผ่าน HTTPS ใน Production
*   **SameSite**: ป้องกัน CSRF Attacks

---

## 🔒 Data Protection & Privacy (การปกป้องข้อมูล)

### 1. Environment Policy
*   **Strict Access**: ข้อมูล Sensitive (เช่น Database Password, API Keys) จะถูกเก็บใน Environment Variables (`.env`) เท่านั้น ไม่มีการ Hardcode ใน Source Code
*   **Validation**: ระบบมีการตรวจสอบความครบถ้วนของ Environment Variables ก่อนเริ่มทำงาน หากไม่ครบระบบจะหยุดทำงานทันที (Fail Fast)

### 2. Caching & Storage (Redis)
*   ใช้ **Redis** ในการจัดการ Session State และ Rate Limiting
*   ข้อมูลใน Cache มีการกำหนด TTL (Time-To-Live) เพื่อไม่ให้ข้อมูลค้างอยู่ในระบบนานเกินความจำเป็น

---

## 👁️ Auditability (การตรวจสอบ)

### 1. Audit Logs
ทุกกิจกรรมสำคัญในระบบ (เช่น การ Login, การแก้ไขข้อมูลสำคัญ) จะถูกบันทึกอย่างละเอียด
*   **Data Integrity**: บันทึกข้อมูลแบบมีโครงสร้าง (Actor, Action, Target, Result)
*   **Traceability**: สามารถตรวจสอบย้อนหลังได้ว่าใครทำอะไร เมื่อไหร่ และผลลัพธ์เป็นอย่างไร

### 2. Request Identification
*   ทุก API Request จะถูกกำกับด้วย `X-Request-Id` เพื่อให้สามารถติดตาม Flow การทำงานเมื่อเกิด Error หรือปัญหาทางเทคนิคได้ง่าย
